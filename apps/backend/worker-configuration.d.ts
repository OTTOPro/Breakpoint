/**
 * Ambient env typing for the Worker.
 * (In later steps this can be regenerated via `wrangler types`.)
 */
interface Env {
  SESSION_DO: DurableObjectNamespace;
  /** D1 — anonymous device identity (V2 Phase 1). */
  DB: D1Database;
}
