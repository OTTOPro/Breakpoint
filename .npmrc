# Expo + React Native do not handle pnpm's isolated symlinked node_modules well.
# The official Expo monorepo guidance is to use a hoisted node-linker so that
# native tooling (Metro, autolinking, prebuild) can resolve packages reliably.
node-linker=hoisted
